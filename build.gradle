apply plugin: "groovy"

group = "com.directmyfile"
version = "0.1.0"

ext {
    indy = false
}

repositories {
    jcenter()
}

dependencies {
    compile "org.codehaus.groovy:groovy-all:2.3.0-beta-2${indy ? ":indy" : ""}"
}